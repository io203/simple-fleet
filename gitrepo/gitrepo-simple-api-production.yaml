apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: simple-api
  namespace: fleet-local
spec:
  repo: "https://github.com/io203/simple-fleet.git"
  branch: main
  paths:
    - deploy
  targets:  
  - clusterSelector:
      matchLabels:
        env: production